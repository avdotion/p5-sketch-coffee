{
  "name": "p5-sketch-coffee",
  "version": "1.0.0",
  "description": "All-in-one package for building sketches. The package uses: * Jade (instead of `html`) * Stylus (instead of `css`) * CoffeeScript (instead of `javascript`) * Gulp (livereload, autocompile)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "coffee-script": "^1.12.7",
    "gulp": "^3.9.1",
    "gulp-clean": "^0.4.0",
    "gulp-coffee": "^3.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-connect": "^5.6.1",
    "gulp-jade": "^1.1.0",
    "gulp-stylus": "^2.7.0",
    "run-sequence": "^2.2.1",
    "uglify-js": "^3.4.9"
  }
}
